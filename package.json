{
  "name": "follow-me-back",
  "version": "0.0.0",
  "description": "Look who is not following you back.",
  "repository": "https://github.com/carlosrive94/follow-me-back",
  "devDependencies": {
    "bower": "^1.7.7",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "postinstall": "bower install",

    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1 ./app"
  }
}
